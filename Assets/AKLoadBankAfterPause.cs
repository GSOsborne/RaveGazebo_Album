using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AKLoadBankAfterPause : MonoBehaviour
{

    AkBank bankLoader;
    // Start is called before the first frame update
    void Start()
    {
        //bankLoader.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
